@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <div style="position:fixed; right:35px; bottom:25px;">
    <a class="btn btn-success text-white" asp-controller="@Model" asp-action="Create" ><i class="bi bi-plus-circle"></i> Add New </a>
</div>
}